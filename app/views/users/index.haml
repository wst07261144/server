-provide(:title, 'All users')
%h1 All users
=will_paginate
%ul(class="users")
  -#-@users.each do |user|
  -#  =render user
  =render @users  #Rails会把 @users 当作一系列的 User 对象，遍历这些对象,然后使用 _user.haml 渲染每个对象
=will_paginate